#include "main.h"
/**
 * _abs - function that computes the absolute value of an int
 * n - Integer tb computed
 *
 * return: the absolute value of the int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
