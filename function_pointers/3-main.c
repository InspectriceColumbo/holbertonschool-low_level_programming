#include "3-calc.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * main - Performs simple operations
 * @argc: Number of arguments 2B passed
 * @argv: Pointer to the array of arguments
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	int num1, num2;
	int (*calcul)(int, int);

	if (argc != 4)
	{
		printf("Error\n");
		exit(98);
	}

	num1 = 
	num2 =
	calcul =

	if
	{
		printf("Error\n");
		exit(99);
	}

	if
	{
		printf("Error\n");
		exit(100);
	}
